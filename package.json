{
  "name": "turing-fullstack",
  "private": true,
  "scripts": {
    "format": "prettier --write 'api/{src,test}/**/*.ts' 'app/src/**/*.{ts,tsx}'"
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "lint-staged": "^8.1.5",
    "prettier": "^1.16.4"
  },
  "workspaces": [
    "api",
    "app"
  ]
}